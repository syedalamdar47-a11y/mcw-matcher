# Deliberately small. Every dependency is a thing that can break at 3am, and a
# thing that can read the process memory holding a practice credential.
playwright==1.59.0
cryptography>=43.0.0
httpx>=0.27.0
